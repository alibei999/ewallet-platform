You are a senior React + TypeScript developer. We are building an eWallet + Payment Gateway MVP (diploma project).

TECH STACK:
- Frontend: React 18 + Vite + TypeScript + TailwindCSS
- HTTP Client: axios
- Routing: react-router-dom v6
- State: React Context (Auth) + useState/useEffect
- Forms: controlled components (no external form lib)
- Icons: lucide-react

DESIGN THEME (dark fintech — apply everywhere):
- Background: #0a0a0a
- Surface: #111111
- Card: #1a1a1a
- Border: #222222
- Accent: #6366f1 (indigo)
- Success: #22c55e
- Error: #ef4444
- Text primary: #ffffff
- Text muted: #9ca3af
- Font: Inter (import from Google Fonts)

BACKEND API BASE URL: http://localhost:8080/api/v1

SETUP COMMANDS to run first:
cd frontend
npm create vite@latest . -- --template react-ts --force
npm install
npm install axios react-router-dom lucide-react
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p

Create frontend/.env:
VITE_API_URL=http://localhost:8080/api/v1

TASK - Create these files with COMPLETE code:

1. index.html (with Inter font link)
2. vite.config.ts
3. tailwind.config.js (extend with all design tokens)
4. postcss.config.js
5. src/index.css (Tailwind + Inter + CSS vars)
6. src/types/index.ts with interfaces:
   - User { id, email, full_name, role, is_active }
   - WalletBalance { currency, balance, locked_balance }
   - Transaction { id, type, status, amount, fee, currency, description, created_at }
   - KYCStatus { status, full_name?, created_at? }
   - Merchant { id, business_name, api_key, webhook_url, is_active }
   - Invoice { id, order_id, amount, currency, status, description, expires_at, paid_at?, created_at }

7. src/api/axios.ts
   - Base URL: import.meta.env.VITE_API_URL || 'http://localhost:8080/api/v1'
   - Request interceptor: add Authorization Bearer token from localStorage
   - Response interceptor: on 401 → refresh token → retry → else redirect /login

8. src/api/auth.ts (register, login, logout, refresh, getMe)
9. src/api/wallet.ts (getBalance, deposit, withdraw)
10. src/api/transfer.ts (sendTransfer)
11. src/api/transactions.ts (getAll, getById)
12. src/api/kyc.ts (submit, getStatus)
13. src/api/merchant.ts (register, getInfo, getInvoices, createInvoice, getWebhookLogs)

14. src/context/AuthContext.tsx
    - Store: user, isAuthenticated, isLoading
    - On mount: restore from localStorage
    - login(), logout(), setUser()
    - Export: AuthProvider, useAuth

15. src/components/LoadingSpinner.tsx
16. src/components/ErrorMessage.tsx
17. src/components/StatCard.tsx (balance display card)

Write COMPLETE code. No placeholders.

Continue building the eWallet frontend. 
Design theme and API endpoints are same as before.
All previous files (axios, AuthContext, types) are already created.

TASK - Create these files with COMPLETE code:

1. src/components/ProtectedRoute.tsx
   - Redirect to /login if not authenticated

2. src/components/RoleRoute.tsx
   - Check user.role === required role
   - Redirect to / if wrong role

3. src/components/TopBar.tsx
   - Show user email on left
   - Logout button on right
   - Call logout() from AuthContext

4. src/components/Sidebar.tsx
   Navigation links with lucide-react icons:
   - Dashboard → LayoutDashboard → /
   - Wallet → Wallet → /wallet
   - Transfer → ArrowLeftRight → /transfer
   - Deposit → ArrowDownCircle → /deposit
   - Withdraw → ArrowUpCircle → /withdraw
   - Transactions → ClipboardList → /transactions
   - KYC → ShieldCheck → /kyc
   - Merchant → Store → /merchant
   - Crypto → Bitcoin → /crypto
   - Settings → Settings → /settings
   
   Admin section (show ONLY if user.role === 'ADMIN'):
   - Users → Users → /admin/users
   - KYC Moderation → ShieldCheck → /admin/kyc
   - All Transactions → ClipboardList → /admin/transactions
   
   Active link style: bg-[#6366f1] text-white
   Inactive: text-[#9ca3af] hover:text-white hover:bg-[#1a1a1a]

5. src/components/Layout.tsx
   - Left sidebar 240px fixed
   - Main content area with TopBar + page content
   - Dark theme

6. src/App.tsx with ALL routes:
   Public: /login, /register
   Protected: /, /wallet, /transfer, /deposit, /withdraw,
              /transactions, /transactions/:id, /kyc,
              /merchant, /crypto, /settings,
              /invoices/:id/pay
   Admin only: /admin/users, /admin/kyc, /admin/transactions
   Catch all: * → NotFound

7. src/pages/Login.tsx
   - Email + Password fields
   - Loading state on submit
   - Error message display
   - Link to /register
   - On success: redirect to /

8. src/pages/Register.tsx
   - Full Name + Email + Password + Confirm Password
   - Loading state
   - On success: redirect to /login with success message

9. src/pages/NotFound.tsx
   - "404 - Page not found" 
   - Back to home button
   - Dark fintech style

STYLING RULES:
- Cards: bg-[#1a1a1a] border border-[#222222] rounded-xl p-6
- Inputs: bg-[#111111] border border-[#222222] text-white rounded-lg px-4 py-3 w-full focus:outline-none focus:border-[#6366f1]
- Primary button: bg-[#6366f1] hover:bg-[#5558e3] text-white font-semibold py-3 px-6 rounded-lg
- Labels: text-[#9ca3af] text-sm font-medium mb-2

Write COMPLETE code. No placeholders.

Continue building the eWallet frontend.
All previous files (Layout, Auth, Context, API) are already created.

TASK - Create these pages with COMPLETE code:

1. src/pages/Dashboard.tsx
   - Welcome: "Welcome back, {full_name}"
   - KYC warning banner if status != approved
   - Wallet balance cards grid (KZT, USD, EUR, RUB) using StatCard
   - Stats row below balances:
     * Total deposited (sum from transactions where type=deposit)
     * Total spent (sum where type=withdrawal or transfer)
     * This month count
   - Quick actions: Send → /transfer, Deposit → /deposit, Withdraw → /withdraw
   - Recent 5 transactions table: date, type badge, amount, status badge

2. src/pages/Wallet.tsx
   - Balance cards for each currency (KZT, USD, EUR, RUB)
   - Each card: currency flag emoji, name, balance, locked balance
   - Refresh button
   - Show 0.00 if no balance

3. src/pages/Transfer.tsx
   - Form: recipient email, currency select, amount, description
   - Fee preview: calculate 0.5% as user types → "Fee: X.XX KZT"
   - Confirmation step before submit
   - Success: show transaction ID

4. src/pages/Deposit.tsx
   - Currency select (KZT/USD/EUR/RUB)
   - Amount input
   - Method select (card / bank_transfer — mock)
   - Submit → POST /wallet/deposit
   - Success message with transaction ID

5. src/pages/Withdraw.tsx
   - Currency select, amount
   - Method select (bank / crypto)
   - Destination field (account number or crypto address)
   - KYC warning if not approved (disable submit)
   - Submit → POST /wallet/withdraw

6. src/pages/Transactions.tsx
   - Table: Date, Type, Amount, Currency, Fee, Status, Description
   - Type badges: deposit=green, withdrawal=red, transfer=indigo, payment=yellow, refund=purple
   - Status badges: success=green, pending=yellow, failed=red, cancelled=gray
   - Click row → navigate to /transactions/:id
   - Loading and empty states

7. src/pages/TransactionDetail.tsx
   - Load single transaction by ID
   - Show all fields in detail card
   - Back button → /transactions

8. src/pages/KYC.tsx
   - Check status on load
   - not_submitted or rejected: show form (full_name, date_of_birth, id_number, photo_url text field)
   - pending: "Your KYC is under review" yellow banner
   - approved: "✅ KYC Approved" green banner

STYLING RULES (same as before):
- Cards: bg-[#1a1a1a] border border-[#222222] rounded-xl p-6
- Inputs: bg-[#111111] border border-[#222222] text-white rounded-lg px-4 py-3 w-full
- Primary button: bg-[#6366f1] hover:bg-[#5558e3]
- Amount fields: type="number" step="0.01" min="0"
- Dates: toLocaleDateString()

Write COMPLETE code. No placeholders.

Continue building the eWallet frontend.
All previous files are already created.

TASK - Create these pages with COMPLETE code:

1. src/pages/Merchant.tsx
   Three tabs: Info | Invoices | Webhook Logs
   
   If no merchant registered:
   - Show registration form: business_name, webhook_url
   - Register button → POST /merchant/register
   
   Tab 1 - Info:
   - Business name, status badge
   - API Key field with show/hide toggle + copy button
   - Webhook URL
   
   Tab 2 - Invoices:
   - Table: order_id, amount, currency, status badge, created_at
   - Create invoice form: order_id, amount, currency, description, expires_in_minutes
   - Submit → POST /merchant/invoice with X-API-Key header
   
   Tab 3 - Webhook Logs:
   - Table: event, status, attempts, delivered (yes/no badge), created_at
   - GET /merchant/webhooks

2. src/pages/PayInvoice.tsx (/invoices/:id/pay)
   - Load invoice: GET /invoices/:id/status
   - Show: order_id, amount, currency, description, expires_at
   - Pay button → POST /invoices/:id/pay
   - Success: "Payment successful!" green message
   - Expired/paid: show status message, disable pay button

3. src/pages/Crypto.tsx (/crypto)
   Two tabs: Deposit | Withdraw
   
   Tab 1 - Deposit:
   - Currency select: BTC / USDT
   - Show mock deposit address based on currency:
     BTC: "bc1qxy2kgdygjrsqtzq2n0yrf2493p83kkfjhx0wlh"
     USDT: "TN3W4T9XYXK2K5V5U1TBD1P5WYGQMFZ7J"
   - Copy address button
   - QR code placeholder (gray box with "QR Code" text)
   - Warning: "Send only BTC/USDT to this address"

   Tab 2 - Withdraw:
   - Currency select (BTC / USDT)
   - Amount input
   - Destination address input
   - Network fee display: BTC=0.0001, USDT=1 USDT
   - Submit → POST /crypto/withdraw
   - Success message

4. src/pages/Settings.tsx (/settings)
   Two sections:
   
   Profile section:
   - Full name input (editable)
   - Email (read-only)
   - Phone input
   - Save → PUT /auth/profile
   
   Change password section:
   - Current password
   - New password
   - Confirm new password
   - Submit → PUT /auth/password
   - Validation: new passwords must match

5. src/pages/admin/AdminUsers.tsx (/admin/users)
   - Load all users: GET /admin/users
   - Table: email, full_name, role badge, status, created_at
   - Block button → POST /admin/users/:id/block (red)
   - Unblock button → POST /admin/users/:id/unblock (green)
   - Show current logged admin cannot block themselves

6. src/pages/admin/AdminKYC.tsx (/admin/kyc)
   - Load all KYC requests: GET /admin/kyc
   - Filter tabs: All | Pending | Approved | Rejected
   - Table: full_name, id_number, status badge, created_at
   - Approve button → POST /admin/kyc/:id/approve
   - Reject button + reason textarea → POST /admin/kyc/:id/reject
   - Pending requests highlighted

7. src/pages/admin/AdminTransactions.tsx (/admin/transactions)
   - Load all transactions: GET /admin/transactions
   - Filter by type select + status select
   - Table: id (short), type badge, amount, currency, status badge, created_at
   - Total count display

STYLING RULES (same as before):
- Cards: bg-[#1a1a1a] border border-[#222222] rounded-xl p-6
- Inputs: bg-[#111111] border border-[#222222] text-white rounded-lg px-4 py-3 w-full
- Primary button: bg-[#6366f1] hover:bg-[#5558e3]
- Tab active: border-b-2 border-[#6366f1] text-white
- Tab inactive: text-[#9ca3af] hover:text-white
- All loading states with spinner
- All empty states with message

Write COMPLETE code. No placeholders.